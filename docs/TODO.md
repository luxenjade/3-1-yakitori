会計フローがフルにSupabase経由で動くことが確認できました。DB行ロック（`SELECT FOR UPDATE`）による在庫管理と、注文の`orders`テーブルへの記録が本番相当で機能しています。

## 現在地の整理

| 機能                                   | 状態                                         |
| -------------------------------------- | -------------------------------------------- |
| 商品一覧・在庫のリアルタイム同期       | ✅ 確認済み                                  |
| スタッフ認証・ロール制御               | ✅ 確認済み（4アカウント作成・ログイン成功） |
| 会計（POS → `checkout` Edge Function） | ✅ **今回確認済み**（`DELETE`バグ修正込み）  |
| 仮注文作成（モバイル注文画面）         | ❌ 未対応（`mockStore`のまま）               |
| 調理ステータス変更（キッチン画面）     | ❌ 未対応（`mockStore`のまま）               |
| 受け取り確認                           | ❌ 未対応（`mockStore`のまま）               |
| サイネージ表示                         | ❌ 未対応（`mockStore`の状態を見ているだけ） |

つまり今は「**会計だけ本番DB、それ以外はローカルモック**」という状態です。これだと今回のように`/pos`で会計しても、`/kitchen`画面には注文が一切表示されません（`KitchenPage`は`state.orders`＝`mockStore`のスナップショットを見ているため）。試しに `/kitchen` を開くと「注文待ち...」のままのはずです。

## 次の一手

ここからは`NEXT_STEPS.md`のP0本体、**「フロントエンドの状態管理をSupabase対応に置き換える」**が本丸になります。具体的には：

1. **顧客用の仮注文API**をSupabase側に用意する（現状、匿名ユーザーが安全に仮注文を作るRPC/Edge Functionが存在しない）
2. **`store-context.tsx` を拡張**し、`orders`/`order_items`テーブルもSupabaseから読み込み・Realtime購読するようにする
3. **キッチンのステータス変更・受け取り確認**を、既にある `advance_order_status` / `verify_pickup` Edge Functionを呼ぶように書き換える
